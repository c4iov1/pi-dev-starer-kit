# Tooling Skills

Use these for specialized tool workflows and extending the skill system itself.

- `artifact-analysis` — inspect SQLite, CSV/JSON/JSONL, archives, and directories safely.
- `write-a-skill` — create new Agent Skills with the correct structure.
