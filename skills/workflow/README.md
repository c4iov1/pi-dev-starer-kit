# Workflow Skills

Use these to manage collaboration, issue flow, QA intake, and cross-session continuity.

- `handoff` — compact current context for another agent.
- `triage` — move issues through the project triage state machine.
- `qa` — conversational QA session that files issues.
- `setup-matt-pocock-skills` — install or refresh bundled external skills.
